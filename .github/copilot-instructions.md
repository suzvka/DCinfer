# Copilot Instructions

## General Guidelines
- When using the terminal, avoid interactive/multiline stdin scripts (e.g., python heredoc). Use single-shot commands because VS only returns output after the process exits.